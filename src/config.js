// Change the api_root_url to your localhost link if you wish to
// test the script in your local
var config = {
    "api_root_url" : "https://starwars-backend-jpc.herokuapp.com/"
}
export default config;